/**
 * -----------------------------
 *             BASIC
 * -----------------------------
 * 
 * 1. don't show the link to them who should not see it
 * only show the person/types of user who should see it
 * 2. Do not allow to visit the link by typing on the url
 * use adminRoute that will check whether the user is admin or  not if not admin then redirect to any other page
 * -------------------------------
 *         TO SEND DATA
 * -------------------------------
 * 1. verify JWT token
 * 2. If it is an admin activity. Make sure only admin user is posting data by using verifyAdmin 
 */